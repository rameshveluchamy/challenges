package algorithm;

public class Palindrome {

    private Palindrome() {
    }

    public static boolean isPalindrome(String in) {
        /*StringBuilder sb = new StringBuilder(in);
        if(in.equals(sb.reverse().toString())) return true;*/

        for (char c : in.toCharArray()) {

        }


        return false;


    }
}
